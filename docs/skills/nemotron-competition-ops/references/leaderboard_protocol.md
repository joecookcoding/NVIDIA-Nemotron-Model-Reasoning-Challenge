# Leaderboard Protocol

- Do not promote a run to `submission candidate` unless its configuration and artifacts are reproducible from the repo.
- Treat prompt and reranker wins separately from benchmark noise.
- Prefer a documented slower winner over a faster undocumented one.
- If rules are still pending, label the candidate as `local-only`.

